# Kanban Studio

## Run

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

This creates the static export in `out/` so the backend can serve the built frontend.

## Tests

```bash
npm run test:unit
npm run test:e2e
```
